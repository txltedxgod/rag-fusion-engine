version: '3.8'

services:
  rag-fusion:
    build: .
    container_name: rag-fusion-engine
    ports:
      - "8000:8000"
    environment:
      - APP_ENV=production
      - LOG_LEVEL=INFO
    restart: unless-stopped
